sudo: required
services:
  - docker

script:
  - docker build --tag gtct/cowsay .
  - docker run gtct/cowsay

after_script:
  - docker images
  
before_deploy:
  - docker login -u $DH-USER-ID -p $DH-PASSWORD

deploy:
  provider: script
  script: docker push run gtct/cowsay
  on:
    branch: master
